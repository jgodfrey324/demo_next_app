/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['placehold.co', 'www.dnd5eapi.co'], // add other domains if you use real images later
      },
};

export default nextConfig;
